# Trade Learning

A one-page paper-trading demo: choose an asset, fetch its price, buy or sell with virtual cash, and view holdings.

```bash
cd Paper-trade-peni667-old
npm ci
npm run dev
```

Run `npm run build` from the same directory. Build output is `dist`. The directory name stays the same for existing hosting configuration.
